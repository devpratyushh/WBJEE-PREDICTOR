name: Ping Streamlit App

on:
  schedule:
    # Runs every 15 mins
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: curl -f -s -m 10 https://wbjee-predictor.streamlit.app/_stcore/health > /dev/null
